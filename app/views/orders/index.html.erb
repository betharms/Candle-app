<h2>All Orders</h2>

<% @orders.each do |order| %>
  <p> Name: <%= order.name %> </p>
  <p> Email: <%= order.email %> </p>
  <p>Cart Candles: </p>
  <ul>
    <% order.cart_candles.each do |item| %>
      <li><%= item.quantity %> x <%= item.candle.name %> </li>
    <% end %>
  </ul>
<% end %>
